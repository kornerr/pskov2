name: Pskov

targets:
    PSKOV:
        type: application
        platform: macOS
        scheme: {}
        sources:
          - path: src/PskovApp.swift
        settings:
            base:
                INFOPLIST_FILE: $(PROJECT_DIR)/Info.plist
