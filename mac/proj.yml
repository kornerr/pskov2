name: PSKOV

targets:
    PSKOV:
        type: application
        platform: macOS
        scheme: {}
        sources:
          - path: PSKOVApp.swift
        settings:
            base:
                INFOPLIST_FILE: $(PROJECT_DIR)/Info.plist
